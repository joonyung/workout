{
  "name": "personal-workout-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.ts",
    "build": "node scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "validate:data": "node scripts/validate-data.ts",
    "coach:context": "node scripts/coach-context.ts",
    "deploy:macmini": "bash scripts/deploy-macmini.sh"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
